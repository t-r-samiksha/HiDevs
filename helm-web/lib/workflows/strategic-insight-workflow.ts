// Real Mastra workflow: runs the 5 strategic-insight engines (decision velocity,
// recurring blockers, commitment drift, meeting ROI, cross-project) as steps.
// Canonical definition in lib/mastra/workflows/, registered in the instance.
export { strategicInsightWorkflow } from "../mastra/workflows/strategic-insight-workflow";
