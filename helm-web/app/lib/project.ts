// Canonical project/workspace the demo runs against. Matches the id the
// pipeline writes items under (see app/api/pipeline/route.ts).
export const PROJECT_ID = "a1b2c3d4-0000-0000-0000-000000000001";
// Integrations are keyed by workspace; this project doubles as the workspace.
export const WORKSPACE_ID = PROJECT_ID;
